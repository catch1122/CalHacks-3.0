using UnityEngine;
using System.Collections;

public class box01 : MonoBehaviour {

	// Use this for initialization
	void Start () {
		GetComponent<Renderer>().material.color = new Color(0.5f,1,1); //C#
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
